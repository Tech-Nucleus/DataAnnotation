[pytest]
# Hard cap per test (requires pytest-timeout) so CI and local runs do not hang indefinitely.
timeout = 120
timeout_method = thread
markers =
    stress: probabilistic aggregation acceptance / stress harness (run via scripts/run_probabilistic_aggregation_stress.sh)
